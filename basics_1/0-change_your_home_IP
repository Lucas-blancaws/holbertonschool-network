#/usr/bin/env bash
# On remplace l'IP 127.0.0.1 par 127.0.0.2 partout où elle se trouve
sudo sed -i 's/127.0.0.1/127.0.0.2/g' /etc/hosts

# On ajoute la ligne facebook
echo "8.8.8.8 facebook.com" | sudo tee -a /etc/hosts > /dev/null